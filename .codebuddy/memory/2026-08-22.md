# 2026-08-22

## 处理 MAP_OPERATION_ISSUES.md 遗留项

文档 docs/MAP_OPERATION_ISSUES.md 记录 M1–M5 地图操作缺陷修复（之前会话已完成代码，但未验证）。

### 遗留项 1（抽象公共 helper）— 已完成
- 新增 `packages/maplibre/src/sourceUtils.ts`：`upsertSource(mlMap,id,data)`（存在则 setData 否则 addSource）、`removeSourceSafe` / `removeSourcesSafe`。从 `maplibre/src/index.ts` re-export。
- 10 个 System 的 `if(!mlMap.getSource)addSource` 样板替换为 helper 调用：grid(GridTopology/loadClass/outageClass)、water(RiverSystem)、pipeline(PipelineTopology/LeakagePlume)、telecom(CellCoverage)、compute(ComputeNodes)、transport(TrafficFlow/RoadNetwork)。
- 关键坑：`@caoguo/maplibre` 的 `exports` 指向 `./dist/*`，值导入必须 `pnpm --filter @caoguo/maplibre build` 后 dist 才含新导出，否则下游报 `is not a function`。

### 遗留项 3（WebGL 降级文案区分）— 已完成
- `apps/demo/common/MapDemo.vue` 与 `FeatureShowcase.vue`：新增 `webglErrorKind`（`'unavailable'` 构造失败/伪上下文 | `'lost'` 运行时上下文丢失），模板按 kind 渲染不同修复引导。

### 配套修复
- 新增 `tools/maplibre-test-setup.ts`：jsdom 补齐 `window.URL.createObjectURL`（maplibre-gl 顶层注册 Worker 所需）。grid/telecom/pipeline/water 的 `vitest.config.ts` 加 `setupFiles`。

### 验证
- `pnpm test` 全量全绿（grid16/telecom11/pipeline61/water21/maplibre76/ai89/compute8/transport19/theme5，0 失败）。
- `pnpm --filter demo build` 成功。
- 文档第四节标记已处理 + 新增第五节处理记录。

### 项目约定（强化）
- `@caoguo/maplibre` 以 dist 形式被下游 import，**任何新增/修改导出的改动必须重新 build maplibre 包**，否则下游测试/运行报 `is not a function`。
- jsdom 测试如需 import 依赖 maplibre-gl 运行时的模块（值导入 `@caoguo/maplibre`），必须配 `setupFiles` 提供 `createObjectURL` polyfill。
- 遗留项 2（辉光 T6 性能，逐 pass 全量重绘）为观察项，本次未改动。
