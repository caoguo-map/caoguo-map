# 环境变量（含密钥，禁止提交）
.env
.env.local
.env.*.local

# Node
node_modules/
*.log

# 构建产物
dist/
*.tsbuildinfo
**/.vitepress/.temp/
**/.vitepress/cache/

# tsup 临时生成物
tsup.config.bundled_*.mjs

# Docker
docker/.env

# 编辑器
.DS_Store
.vscode/
.idea/
.codebuddy
# 误入的构建参数目录（如 tsup --no-clean 被当作路径写入）
--no-clean/
