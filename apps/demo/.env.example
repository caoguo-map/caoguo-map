# 演示中心前端环境变量模板
#
# 复制本文件为 `.env`（已被 .gitignore 忽略，不会被提交）：
#   cp .env.example .env
# 然后填入你自己的天地图 token。

# 天地图（Tianditu）授权 token。
# 默认底图会从 OpenStreetMap 切换为天地图（国内权威底图，CGCS2000 / Web Mercator）。
# 免费申请：https://console.tianditu.gov.cn/api/key
# 未配置时自动回退 OpenStreetMap。
VITE_TIANDITU_TOKEN=

# （可选）AI 代理服务地址，供前端 /api 代理转发（默认 http://127.0.0.1:8787）。
# CAOGUO_AI_PROXY=http://127.0.0.1:8787
